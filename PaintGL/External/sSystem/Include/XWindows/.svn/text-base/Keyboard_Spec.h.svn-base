#ifndef __Keyboard_Spec_h__
#define __Keyboard_Spec_h__

#include "Keyboard.h"

class Keyboard_MSC : public Keyboard
{
public:
  Keyboard_MSC(void) : Keyboard() { return; }
  ~Keyboard_MSC(void) { return; }

private:
  Keyboard_MSC(const Keyboard_MSC &);
  Keyboard_MSC &operator=(const Keyboard_MSC &);
}; // Keyboard_MSC

#endif // __Keyboard_Spec_h__
